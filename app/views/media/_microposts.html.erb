
<%= form_for(@micropost) do |f| %>
  <div class="field">
    <%= f.text_area :content, placeholder: "New comment..." %>
  </div>
  <%= f.submit "Post" %>
<% end %>

<%= @microposts.each do |micropost| %>
<li>
  <p class="comment_body">"<%= micropost.body %>"</p>
  <br/>
  <p class="comment_info"><%= micropost.name %> - <%= time_ago_in_words(comment.created_at) %> ago </p>
</li>
<% end %>


<ul class="microposts">
  <% if @post.microposts.empty? %>
    <li>none yet</li>
  <% else %>
    <%= render 'microposts' %>
  <% end %>
</ul>



<li>
  <span class="content"><%= micropost.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
  </span>
</li>